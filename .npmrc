# npm 12 disables git-protocol dependencies by default (EALLOWGIT). This repo
# pins `poetic` as a top-level git dependency, so allow git installs scoped to
# root-level dependencies only (not transitive) — see TECH-DEBT.md TD26071804.
allow-git=root
