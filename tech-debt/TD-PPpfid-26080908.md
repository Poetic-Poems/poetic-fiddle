---
id: TD-PPpfid-26080908
title: Route-heading JSX pattern still duplicated across four pages; the prior review's fix recommendation was never tracked
status: open
filed: 2026-08-09
review: project-review-2026-08-08 R-09
---

*Filed 2026-08-09, from the 2026-08-08 project review (R-09, F-CODE-01).*
Severity: Low. `src/components/PageHeader.tsx` (a `{title, lastUpdated}`
component) exists and is used by the three legal pages, but four other
route headers still hand-roll the same
`<h1 className="font-serif text-2xl font-semibold tracking-tight">` +
description-`<p>` block inline: `src/app/page.tsx:8-13`,
`src/app/poems/[id]/page.tsx:14-19`, `src/app/poems/page.tsx:9-20` (which
has since gained `id`/`tabIndex` attributes the other three don't carry,
for TD-PPpfid-26080102's focus-target need), and
`src/app/remix/[share_id]/page.tsx:41-50`. The 2026-07-31 project review
recommended (R-13) extracting a second `PageHeader`-style component for
this pattern, but — unlike every one of that review's other
recommendations, all of which have a `ref:`-resolved tech-debt item — that
recommendation was never converted into a register entry, so it dropped
off the project's radar even though all four call sites have been touched
by later PRs without picking it up.

Fix: extract a `PageHeader`-sibling (e.g. `{title, description}`) covering
this shape and use it at all four call sites. See
`reviews/project-review-2026-08-08/04-improvement-prompts.md` R-09.
