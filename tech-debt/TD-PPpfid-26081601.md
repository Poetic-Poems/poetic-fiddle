---
id: TD-PPpfid-26081601
title: scripts/td-check.pl has drifted from Poetic-Poems/poetic's current copy
status: resolved
filed: 2026-08-16
resolved: 2026-08-17
ref: 332
---

*Filed 2026-08-16, from the review of PR #327
(https://github.com/Poetic-Poems/poetic-fiddle/pull/327#issuecomment-5305165262).*

`scripts/td-check.pl` — untouched by PR #327, which only added
`reserve-tech-debt-id.pl` and `check-tech-debt-open-rewrites.pl` to
`.github/workflows/td-tooling-drift.yml`'s watch list — has drifted from
`Poetic-Poems/poetic`'s current `main`. Upstream has since added a check
that an `open`/`in-progress` item's body contains non-whitespace text
(resolved/not-debt items are exempt, to grandfather legacy items migrated
with an empty body), with `my $live = $st eq 'open' || $st eq 'in-progress'`
(line 132) hoisted out of the status branch to support it; this repo's copy
predates that and still only checks id/title/status/filed consistency
(lines 15-16).

`td-tooling-drift.yml` last ran green on 2026-08-10, so the drift arrived
upstream after that; its next scheduled run (Mondays 06:17 UTC) will go red
over this and auto-file a drift issue on its own — that is the workflow
behaving as designed, not a defect, but the review asked for this filed
ahead of that rather than left to the automatic detection.

The review confirmed upstream's current `td-check.pl` passes unchanged
against this repo's register as of 2026-08-16 (90 items, 4 open,
consistent), so the bump is behaviour-neutral here today; it was left out
of PR #327 to keep that PR scoped to the two new scripts.

Fix: copy upstream's current `scripts/td-check.pl` byte-identically into
this repo, mirroring how `reserve-tech-debt-id.pl` and
`check-tech-debt-open-rewrites.pl` were mirrored in PR #327 — re-run
`perl scripts/td-check.pl` against the live register afterwards to confirm
it still passes.

Where: `scripts/td-check.pl`, watched by
`.github/workflows/td-tooling-drift.yml`.
